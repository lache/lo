html
  include ./includes/head.pug
  body.mar00.fwht.bsea.f1
    include ./includes/actionresult.pug
    a.dpinblk.red.pad2(href='script:ttl_go_back()') 뒤로
    h2.semitransblack.tc 선장 고용
    div.tc
      img(src=captainTemplate.portrait)
    div.tc 고용비용: #{captainTemplate.price}
    div.tc 월급: #{captainTemplate.salary}
    if captainTemplate.maxLoad
      div.tc 최대적재량 증가: #{captainTemplate.maxLoad}
    if captainTemplate.moveSpeed
      div.tc 이동속도 증가: #{captainTemplate.moveSpeed}
    if captainTemplate.loadingSpeed
      div.tc 하역속도 증가: #{captainTemplate.loadingSpeed}
    if captainTemplate.durability
      div.tc 내구도 증가: #{captainTemplate.durability}
    div.tc
      a.dpinblk.red.pad2(href='script:confirm_hire_captain(' + ship.ship_id + ',' + captainTemplate.id + ')') 고용하기
      a.dpinblk.red.pad2(href='script:ttl_go_back()') 취소
